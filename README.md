# Payload Posts App

Simple demo showing posts and categories on top of PayloadCMS + Next.js.

## Quick Start

```bash
npm install
npm run dev
```
Open http://localhost:3000

## Login

- Email: `test@test.com`
- Password: `test`

Create posts from the form and they appear immediately below. `/admin` opens the full Payload admin panel.